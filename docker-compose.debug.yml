version: '3.8'

# Debug override - exposes Redis port for monitoring
# Usage: docker-compose -f docker-compose.snapshot-sequencer.yml -f docker-compose.debug.yml up

services:
  redis:
    ports:
      - "6379:6379"  # Expose Redis for debugging/monitoring